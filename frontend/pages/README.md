# Reserved by Next.js

This empty `pages/` directory exists because Next.js will otherwise try to use
`src/pages/` as the Pages Router and break the build, even when the project
uses only the App Router.

The FSD `pages/` layer lives at `src/pages/`. Do not place route files here.
