spring:
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

everytldr:
  visitor:
    hash-secret: test-visitor-secret
    cookie-secure: true
  article-view:
    flush-enabled: false
